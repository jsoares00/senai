programa {
  funcao inicio() {
    real peso, altura, imc 
    escreva("quanto voce pesa ? ")
    leia(peso)
    escreva("qual a sua altura ? ")
    leia(altura)
imc =(peso ) / (altura * 2)
se(imc < 25)(escreva("parabens seu imc esta muito bom\n seu IMC é de :",imc))
se(imc > 25)(escreva("voce esta mal, cuide mais da sua saude\n--seu IMC é de :", imc))
  }
}
